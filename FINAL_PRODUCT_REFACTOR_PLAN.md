# Autonomous Scientific Research Agent 最终产品重构方案

> 审计日期：2026-09-05，Asia/Shanghai。状态：架构提案，待确认后实施。
>
> 本轮交付仅为本方案：不修改前后端业务代码，不新增 API，不编写 Astronomy Adapter，不生成 CSV。
>
> 依据：本次附件最终任务、`AUTONOMOUS_RESEARCH_AGENT_FINAL_ARCHITECTURE.md`、`AUTONOMOUS_RUN_PHASE_A_REPORT.md`、`IA_SUPERNOVA_SOURCE_VALIDATION_REPORT.md`，以及当前仓库代码和专项回归结果。历史文档与现状冲突时，以本次代码审计为准。

## 0. 重构决策摘要与范围

核心判断：项目不是缺少功能页面，而是缺少把既有能力组织成“持续研究 → 自主执行 → 真实资产 → 继续研究”的统一产品运行链。

左侧研究列表和三栏外壳已经存在；继续调整 Copilot 卡片不能解决问题。下一阶段应替换默认产品入口、运行驱动方式、状态归属和结果交付方式，而不是再增加一个与旧 Copilot 并列的展示页面。

本方案作出六项明确建议：

1. 默认产品只有一个 Research Workspace；首页提交问题后直接进入 Research Run，不先进入澄清流程。
2. 后端 `AutonomousResearchRunService` 是唯一编排者；前端提交意图、读取结果、发送控制命令，不串联业务阶段。
3. 区分 Project、Run、Session、Asset；历史、摘要、会话、资产都归属具体研究，不能继续让 `session_id` 兼任所有身份。
4. 以 CfA4 真实观测表打通第二领域；五类可下载资产和字段级溯源是完成标准，Graph 不是终点。
5. 默认自动执行低风险、可恢复的科研动作；用户确认只处理具体风险和重大边界变化，不能豁免医学硬规则。
6. 比赛前交付“两个明确支持的领域 + 可验证多模态链 + 可恢复工作区”，不宣称任意领域、任意数据源均可自主整合。

已通过的 Phase A 不作废：它是自主 preview 编排基线，不是最终数据执行器。B0 是已获得的真实来源验证证据，不是已经上线的 Astronomy Plugin。本轮重新规划的是后续完整交付路线。

范围假设与取舍：

- 按单机、单后端实例、单操作者比赛部署设计，同时支持多个研究窗口。若要开放公网多用户，必须先增加真实身份认证和租户授权，不能依赖随机 ID 保密。
- 比赛日期、机器配置、模型额度尚未给出；因此采用依赖和验收门推进，不虚构完成日期或吞吐指标。
- 当前原生 ES Modules 可以承载新产品，不把更换 React/Vue 当成重构前提。
- 最新附件要求完整后端运行时和持久化，已超出历史“前端 Phase1、不新增 API”的范围。本文提出后续增量 API；本轮不实现，须在本方案确认后开展。
- 新路线按附件采用 A 编排、B 天文、C 历史模型、D 工作区、E 资产中心、F Demo。旧架构文档中 E 为模型配置的编排不再沿用；模型配置改为贯穿 A/D/F 的前置事项。

## 1. 当前项目优势：保留哪些已经建成的能力

| 能力 | 当前依据 | 重构中的位置与边界 |
|---|---|---|
| 既有科研执行内核 | `backend/app/agent/`；`docs/AGENT_ARCHITECTURE.md` | 复用规划、采集、质量与导出服务，不复制第二套医学内核；仍需验证在新 Run 中真实接通 |
| 医学来源适配和规则 | 既有 GDC、GEO、cBioPortal、AACT、CIViC、DepMap 模块及测试 | 保留领域能力和受控访问边界，不为天文演示修改医学事实语义 |
| v30 研究能力分层 | `backend/app/v30/` 的 runtime、planner、registry、discovery、selection、schema、graph、integration | 作为内部能力复用；目录候选、字段假设、执行结果保持不同状态 |
| Phase A 自主编排 | `backend/app/v31/service.py`、`stages.py`、`models.py`、`api.py` | 已有后端 worker、Run、事件、失败短路、暂停/取消入口，无需从零再写 Orchestrator |
| 真实 Ia 来源前置验证 | B0 报告及 `tmp/source_validation/` 原始响应 | 有明确表头、行数、redshift 来源和确定性关联方案，避免盲写适配器 |
| 三栏与研究切换雏形 | `frontend/v31/index.html`、`app/workspace-store.js`、`components/research-list.js` | 可复用布局、组件和文字处理，但内存槽位不能代替持久研究模型 |
| 解析与原始值结构 | `backend/app/parsers/registry.py`、`base.py` | CSV/TSV、Excel、HTML、JATS、已有文本层 PDF 的基础结构可复用，保留原字段、原值、位置、解析版本 |
| 已有导出服务 | `backend/app/agent/exporter.py` | 已支持数据、Metadata、Quality Report；需要封装成绑定 Run 的持久资产，不是从零写所有序列化逻辑 |
| 科研安全基线 | 三份冻结配置、`docs/06_评测指标与SDTI.md` 和既有回归 | 作为硬约束继续生效；新增天文规则单独版本化 |

本次实际运行：`python -m pytest -o addopts='' -q backend/tests/autonomous_run backend/tests/v30 backend/tests/v31_frontend`，结果 **135 passed，1 warning，39.00 秒**；解释器为 Python 3.14.4。warning 为现有 Starlette/httpx TestClient 弃用提示，本轮未修改依赖。

这不是全量后端回归、生产容器验收或浏览器 E2E。本次只做代码/文档/保存的 B0 证据审计及上述专项测试；未重新执行真实外网取数，也未声称完成视觉实测。历史报告中的测试数量不作为本轮新成绩。

工作区快照说明：审计期间 `backend/app/agent/exporter.py` 出现了新增独立来源 ZIP 导出的差异，本助手未写入或回滚该文件。该变化不等于已建立 Project/Run/Asset 持久闭环；上述测试结果仅适用于执行时快照，不声称已验证随后出现的改动。正式实施前应重新检查工作区差异与相交文件。

## 2. 当前核心问题：产品断点与工程根因

### 2.1 已确认的主要断点

| 优先级 | 审计事实 | 用户后果 | 必须改变的结构 |
|---|---|---|---|
| P0 | `home.js:35` 仍是“开始澄清研究目标”；`copilot.js:129` 再次要求开始，`:255` 单独发起规划 | 输入一句问题后仍要逐次推动流程 | 一次提交创建 Run，普通科研歧义转为可见工作假设 |
| P0 | `main.js:2` 引用 v30 API；主路由仍为 Copilot/Timeline/Sources/Mapping/Graph；没有消费 v31 Run API | 后端已经自主，用户却看不到 | 新 Workspace 读取 Run snapshot/events，旧页面退出默认主链 |
| P0 | `v31/service.py` 终止于 `completed_preview`；`stages.py` 明确 `dataset_generated=false`、`quality_gate_executed=false` | 自动到达预览，不等于完成科研数据工作 | 补 retrieving、真实 mapping/linking、数据质量与资产生成 |
| P0 | `v30/discovery/service.py:24` 只把 Registry 投影成候选；v31 mapping 输入仍来自 `field_hypotheses` | 字段合同看起来完整，但未证明来源真的有字段 | 检索定位与机器字段验证分开；正式 mapping 只消费解析结果 |
| P0 | `v30/schema_generator/service.py` 的 `_SN_FIELDS` 将 `flux` 必填，未包含 `magnitude/error` | 与 B0 实测两来源均无 flux 的事实冲突 | 新天文合同以观测表为依据；flux 可空，不造值 |
| P0 | `workspace-store.js:6` 是进程内前端 Map；Run/Event Store 也是后端内存字典 | 刷新丢历史、服务重启丢运行，Project/Asset 无可靠归属 | 后端持久化、对象关系、恢复协议 |
| P0 | `frontend/Dockerfile` 只复制旧入口和旧脚本；FastAPI 根目录也仍服务 `frontend/index.html` | 本地 `/v31/` 与正式入口可能是两个产品 | 同一发布产物、统一根入口、容器/直启一致验收 |
| P0 | Run 创建请求没有模型凭据引用；v31 页面没有模型配置入口；医学桥接 `_to_agent_request` 固定 `use_qwen=False` | “模型已配置”不等于本次 Run 实际使用模型 | 配置状态、凭据解析、执行模式贯通，逐 Run 记录真实模式 |
| P1 | 右侧摘要从 `plan`、clarification 和 next-action 推断状态，非 Run 结果投影 | 仍反复要求选方向，进展可能滞后 | Research Brief 由目标/假设版本和 Run 状态派生 |
| P1 | `v30/figures/service.py` 仅分类 caption/引用字符串，明确未读像素；`PdfTextParser` 消费已有文本，不读取 PDF 二进制 | 不能把图注分类叫真实图像理解，把文本输入叫完整 PDF 解析 | 多模态能力矩阵、真实文件输入、证据定位和失败状态 |
| P1 | `v31/api.py` 只有创建、详情、事件、暂停、取消五类入口，无追问、恢复、Project/Asset 历史 | “继续研究”无法成为同一研究的增量执行 | 项目消息、版本化派生 Run、恢复/确认决议和资产查询 |

必须纠正旧诊断：左侧已经不是八步节点墙，右侧也已经有摘要。现有问题是研究列表的身份仍是 session、历史只在内存、研究内部仍由确认流程驱动。重构应替换这个运行模型，不再重复“把左侧改成列表”的已完成工作。

### 2.2 尚不能用现有测试证明安全的部分

- **异步切换风险**：`copilot.js:91` 起在请求返回后直接写全局 session-store，没有校验该响应是否仍属于当前研究。`adoptSession` 有部分防护，但不能覆盖 turn/plan 的迟到响应。需用真实延迟与切换 E2E 验证，不把字符串测试当作无污染证明。
- **同领域共享对象风险**：SchemaPackGenerator 以固定 oncology ID/默认 astronomy ID 存储带 `selected_sources` 的对象，且被共享注入。不同 Run 的任务元数据可能相互覆盖；必须区分只读 schema definition 和 Run 专属 binding。现有并发测试主要验证问题、session、事件 ID，不覆盖这些对象。
- **控制竞态风险**：Run Store 的 get/update 各自加锁，但“读取—判断—更新”及事件写入不在同一个事务。暂停/取消与阶段完成并发时存在旧快照覆盖新状态的窗口；现有测试主要覆盖 worker 尚未开始时的控制。
- **风险识别过于词面**：当前 intake 正则看到 IHC 2+ 即等待确认，且没有贯穿取数/关联/导出的行动门禁。仅讨论医学概念与执行高风险判定需要区分；这项升级必须新增回归，不能直接删除医学拦截。
- **源选择到执行未形成强约束**：医学桥接会在选中 cBioPortal 时固定加入 `brca_metabric`，且当前映射不保证用户新排除条件贯穿执行；默认队列选择不能冒充由用户问题推导的唯一研究来源。
- **测试仍冻结旧产品形态**：部分前端测试断言只许调用早期 API、保留 Copilot 路由和旧根入口。实施时应按批准的新产品契约替换这些阶段性断言，同时保留安全和诚实性断言，不能只删除失败测试。

以上是由代码结构识别的风险/覆盖缺口，不冒充本轮已复现全部并发故障。重构阶段必须补复现测试后修复。

## 3. 最终产品形态与用户完整流程

### 3.1 产品主张

用户交给系统的是一个持续研究的问题，不是一张待填表单。系统把问题转为可执行、可审查的研究任务，在明确的能力边界内自动执行，并交付可复用的数据资产。

普通问题默认先采用可逆的工作假设。例如“Ia 型超新星光变曲线”可理解为“先整理公开多波段观测与来源元数据”，不自动夸大成“验证宇宙学因果结论”，也不要求用户预先填齐红移参考系、模型和所有字段。

### 3.2 三栏工作区

```text
Research Workspace       Research Run                         Research Brief
────────────────────     ───────────────────────────────      ──────────────────
+ 新建研究               原始科研问题                         对象 / 目标
                         AI 理解 · 工作假设                   当前阶段
HER2 耐药机制            Agent 计划                           已确认内容
  已有资产               当前正在做什么 / 已完成什么          工作假设（未确认）
                         阶段结果 · 来源 · 证据               风险 / 数据缺口
Ia 超新星光变曲线        结果资产入口                         下一步自动动作
  运行中                 ───────────────────────────────      [技术详情]
                         继续追问 / 修改方向 / 添加材料
历史与归档               [暂停] [取消]（按状态出现）
```

- 左栏只导航研究，不导航算法阶段。项目卡显示标题、最近更新时间、真实运行/资产状态；同一项目历史 Run 在项目内切换。
- 中栏是可阅读的研究记录：短段说明、带来源的结论、阶段结果和数据预览；不排成左右聊天气泡，也不堆满十一张等高流程卡。
- 右栏展示当前研究的简报。已确认假设必须具有确认事件；Agent 暂定理解不能自动写进“用户已确认”。没有确认项时不显示催促确认按钮。
- `route/retrieval_status/constraints/raw API` 放入技术 Drawer。真实风险、未取到数据、数据缺口和质量失败必须在主界面可见，不能一起藏进去。
- 前端展示的是执行摘要与工具证据，不展示模型内部逐字思维过程。

### 3.3 从首次输入到继续研究

1. 打开统一入口，读取模型/后端可用状态。输入问题和可选附件，点一次“开始研究”。
2. 后端原子创建 Project、初始消息、Run 和独立 Session，返回工作区地址；不会先建空会话再要求点一次开始。
3. 工作区立即显示原始问题。理解、计划、发现、取数、处理、质量和资产状态由真实事件逐步出现。
4. 来源失败时解释失败与替代策略；存在安全且已支持的替代来源时自动有限重试，不能无穷搜索或装作完成。
5. 用户可随时切换到 HER2 研究。天文 Run 在后端继续；切回后读取同一 Run 的进度和输出，不重跑。
6. 完成后显示可访问的真实资产版本、行数、来源、主要限制和下载入口。资产未完成时只能下载已存在且标为草稿/审查材料的文件。
7. 用户继续提问，系统按消息意图进行解释、建立增量 Run 或关联分支；不要求重新填写整个研究表单。

### 3.4 追问语义必须明确

| 用户操作 | 后端行为 | 上下文与资产规则 |
|---|---|---|
| “为什么选择这个来源？” | 读取该 Run 的选择决策和证据，生成解释 | 不触发重新抓取；回答引用真实决策，未记录的原因不得编造 |
| “只保留公开数据” | 更新约束版本；若结果需要变化则创建子 Run | 过滤与数据访问分类由后端执行；旧结果仍可追溯，不覆盖 |
| “换一个数据库” | 已指定且支持时创建替代来源 Run；未指定时建议可行替代 | 找不到适配器则说明能力不足；不静默回退到被排除来源 |
| “继续研究红移关系” | 继承已验证资产引用，创建新目标/计划版本及分析 Run | 每个观测不是独立 SN 样本；具体统计分析只有真实实现和验证后才标完成 |
| “扩展到另一个领域” | 明确跨领域时创建关联研究分支，沿用这条用户意图 | 新 research_id/session；只携带明确相关的摘要/证据引用，不复制医学数据或整段上下文 |
| 运行中修改方向 | 记录修订；当前 Run 在安全检查点停止/暂停，再创建子 Run | 不在运行中偷换原始问题；每个 Run 的输入快照不可变 |

比赛版同一 Project 默认一次只有一个数据写入型 Run 活跃；不同 Project 可并行。只读追问不阻塞数据 Run。范围无法确定且会明显改变结果时可问一个必要问题，这不等于恢复逐阶段确认流程。

## 4. 后端架构调整

### 4.1 四种身份与所有权

| 对象 | 最小职责与关键内容 | 关系/不变量 |
|---|---|---|
| Research Project | `research_id`、标题、归属、目标版本、约束版本、摘要版本、创建/更新时间、归档状态 | 长期研究；拥有消息、Runs、Sessions、Assets；不等同于数据库 study_id |
| Research Run | `run_id`、`research_id`、`parent_run_id`、输入快照、目标/合同版本、`session_id`、阶段/状态、事件游标、模型模式、资产引用 | 一次执行；输入不可被后续消息覆盖；输出版本不可变 |
| Session | `session_id`、`research_id`、会话/记忆版本、上下文摘要、允许引用的证据范围 | 执行上下文；Run 可从本项目历史生成新 Session 快照，不读取其他研究的当前内存 |
| Asset | `asset_id`、research/run 归属、类型、版本、状态、格式、大小、哈希、schema/规则版本、provenance 引用 | 文件和 manifest 都存在才可 ready；不能把规划 JSON 作为 dataset |

Project conversation 是可恢复的用户消息/研究回复记录；Run events 是机器执行事实，两者通过 message_id/run_id 引用但不能混为一张临时 UI 日志。Research Brief 持久保存版本或可从这些对象重建，切换时不依赖浏览器旧摘要。

`qwen_session_id` 是短期凭据句柄，不是科研 Session；不能用它替代 Project/Run/session 的任何身份。

### 4.2 单一编排入口，复用内部服务

后端调用链为：Workspace command → AutonomousResearchRunService → 经验证的领域能力 → 原始响应/解析记录 → 质量与证据 → Asset writer。

- 扩展现有 `backend/app/v31/`，保留 Run/Event 基础。阶段处理器调用 Python 服务，不通过 HTTP 回调自身接口。
- 在 planning 输出可执行研究合同：目标、范围、已知/暂定假设、所需字段、数据粒度、支持的工具、来源选择理由、任务预算、预期资产和退出条件。当前只有 kind/topic/notice 的 working plan 不能满足此合同。
- 用户原话中抽取的限制与显式约束合成同一版本化合同，selection、fetch、mapping、join、export 均检查同一合同。
- 医学域由受控委托适配层复用旧执行内核；天文域由新 Astronomy Plugin 执行。不允许“非医学任务也先套医学 CanonicalRecord”。
- 拆分可共享的只读 schema 定义与绑定 run_id 的 schema instance；source binding、mapping、graph、质量结果不得保存在以领域固定 ID 索引的可变全局槽位中。
- 比赛版仅需小型、显式、可测试的 domain binding 表，不建设通用插件市场、动态代码执行或多 Agent 通信平台。

### 4.3 最终阶段与真实产物

| 后端阶段 | 必须产生/验证的内容 | 不允许的替代 |
|---|---|---|
| intake | 原始问题、材料引用、执行模式、预算与初始权限检查 | 修改原话或跳过未配置状态 |
| understanding | 领域/对象/范围、目标与不确定性、工作假设 | 将推测登记成科学事实/用户确认 |
| planning | 可执行合同、能力校验、字段需求、退出条件 | 仅生成“将要研究”的段落 |
| discovering | 真实文献/数据库/表/附件定位与候选状态 | Registry 存在即宣称已经搜索/取数 |
| source_selection | 选择与拒绝原因、来源依赖、访问/引用条件 | 强制凑多源或选不存在的 Adapter |
| retrieving | 真实响应、请求和访问时间、内容哈希、实际字段 | HTTP 200 即成功、HTML 错页当数据 |
| structuring | 解析记录、单位/时间/对象元数据、来源支持的 schema 版本 | 由 LLM 补造数值使 schema 完整 |
| mapping | 根据真实表头/语义的映射、原值、转换与审查记录 | 使用候选 field_hypotheses 冒充原字段 |
| linking | 清洗/关联结果、确定性 join 统计、证据和下载血缘 | 名称相似度猜红移或患者身份 |
| quality_review | 对真实结果运行质量规则，给出通过/警告/阻断及原因 | 图/schema 存在即“质量通过” |
| asset_generation | 写入数据和四类伴随资产，校验 manifest 后提交 | 无数据却生成空壳成功态 |

清洗属于 structuring/mapping/linking 中版本化的数据变换，不额外制造需要用户点击的主阶段。领域内旧执行器若一次调用完成多个子步骤，必须记录真实的 delegated execution 边界，不能伪造细粒度开始/完成时间。长期再按需求拆解旧内核。

### 4.4 控制、失败与恢复

- 区分生命周期状态和当前业务阶段；保留旧 `completed_preview` 兼容语义，新 Run 增加明确的结果级别，不能把所有 `completed` 显示为“已生成数据资产”。
- 推荐状态：queued、running、pause_requested、paused、waiting_for_confirmation、cancel_requested、cancelled、completed、failed、interrupted。控制请求先确认收到，到检查点才显示已暂停/已取消。
- 采用带 revision 的条件更新或等价事务，使阶段结果、Run 状态和事件序列一致；已取消的 Run 不能被迟到结果改回运行/完成。
- 阶段产物以 `(run_id, stage, input_version, attempt)` 归属；重试幂等、事件去重、资产不覆盖。事件按 cursor 增量读取，读取不能推动执行。
- 超时/429/临时网络故障可有限重试；缺列、错误文档、禁止访问、许可/科学语义不明不能当暂时网络故障无限重试。设置来源数量、下载字节/行数、请求并发、阶段时限和模型预算。
- 停止运行不一定能中断正在进行的外部 HTTP；必须诚实显示 stopping，迟到结果只进入隔离记录，不继续生成正式资产。
- 服务重启后未完成 Run 标为 interrupted；检查已提交原始文件和阶段检查点后，仅恢复可重入步骤。不伪称模型凭据随重启恢复，不重复发布外部数据。
- 失败详情返回脱敏 error_code/request_id/可行动说明；不把 `str(exc)` 原样复制到前端、事件或导出包。

### 4.5 持久化与部署取舍

比赛版推荐 SQLite 保存 Project/Run/Session/messages/events/Asset manifest，文件系统保存原始响应与资产；使用持久卷、事务与数据库迁移。大表和二进制文件不塞进事件 JSON，密钥不进业务数据库。

部署数据目录与源码、OneDrive 同步目录分离，不共享一个正在同步的 SQLite 文件；明确单进程 worker 约束和关闭清理机制。多实例队列、分布式数据库是后续部署需求，不是比赛前默认引入的依赖。

### 4.6 API 演进建议（尚未实现）

| 接口面 | 建议职责 |
|---|---|
| 现有 `/api/v31/research-runs` | 保留创建能力，扩展 project 归属、幂等键、执行模式和短期凭据引用；服务端校验归属 |
| Run 详情、events、pause、cancel | 保留并补 cursor、revision、状态控制语义；旧事件可读 |
| Project create/list/detail/update/archive | 持久研究列表、目标/约束版本、摘要；初次开始时可原子创建 Project+Run，避免 UI 二次确认 |
| Project messages | 提交追问，服务端判断只读解释或派生 Run，返回 message/run 引用 |
| Run resume / confirmation resolution | 检查点恢复与具体风险决议；决议绑定 action/rule/input version，不接受通用“全部同意” |
| Project runs / assets、Asset detail/content | 历史执行、资产版本、受控预览/下载，不向浏览器暴露任意文件路径 |
| Material upload / attachment reference | 有限格式、大小、哈希、来源信息、隔离状态，供后端统一多模态管线消费 |
| 现有模型 configuration / qwen-sessions | 优先复用；需要补充的模式字段只返回非敏感状态 |

具体路径和 DTO 在实施阶段以契约测试固定；不同时建设 REST、SSE、WebSocket 三套协议。比赛版先用增量轮询，刷新/断线恢复正确后再考虑流式传输。

### 4.7 自动执行、确认与禁止三分法

| 类别 | 动作 | 默认处理 |
|---|---|---|
| 自动 | 理解、工作计划、公开来源发现、允许来源下载、schema、字段映射、证据投影、质量检查、私有资产生成 | 满足能力、预算、权限和规则后自动执行 |
| 自动且记录规则 | CfA4 同目录 exact SN metadata 多对一关联；明确可逆的格式清洗 | 校验唯一性、基数和缺失，保存原值与变换，不强迫普通确认 |
| 需要定向确认/审查 | 患者级关联、医学高风险行动、会改变结论的不可确定解释、不可逆操作、对外发布 | 展示具体对象、影响、证据与备选；暂停相关分支 |
| 不可由确认豁免 | IHC 2+ 直接判阳性、CNA 等同 IHC、无证据患者匹配、高权威冲突自动选边、捏造数据 | 拒绝违规转换，保留 unresolved/review；“继续”不能解锁 |

患者级 Join 不因“同一个项目”自动获准；先满足现有冻结规则与可验证身份依据，再进入定向审查。医学概念解释不等于高风险执行。所有高风险动作在实际行动前重新检查，而非只扫一次输入关键词。

## 5. 前端架构调整：完整替换主产品链

### 5.1 信息架构和路由

- 默认根入口：新研究输入与历史研究。表单仅科研问题、可选材料、可选范围；高级设置折叠。
- 工作区建议路由：`#/research/{research_id}/runs/{run_id}`。URL 的 research 与 run 均由后端确认，不能继续把 session ID 标成 research ID。
- 资产视图：`#/research/{research_id}/assets/{asset_id}`；保留左侧研究上下文和返回当前 Run 的入口。
- 项目历史：工作区内 Run 版本选择，不另做第二套研究导航。
- 旧 `#/r/{sessionId}/copilot` 仅作为迁移入口。若无 Project 对应关系，显示旧会话的诚实迁移/只读状态，不编造历史运行和不存在的 CSV。
- 旧工作台保留为明确标记的 legacy/诊断入口，不再与新产品同时争夺首页；容器和 FastAPI 根路由一起切换。

### 5.2 组件取舍

| 当前组成 | 重构后职责 |
|---|---|
| Home / Copilot | 替换为问题入口和 Run 叙事，不保留强制方向卡链 |
| Timeline | 变成 Run 中的真实活动记录；十一后端阶段按用户任务分组，不作为导航门禁 |
| Sources | 来源选择与下载结果详情面板；展示为何选择/拒绝及真实取数结果 |
| Mapping | 字段映射和质量审查详情；不再要求手动点击才能启动映射 |
| Graph / Why drawer | 嵌入证据与资产视图，保留溯源解释交互；不展示无证据的事实边 |
| Research list / Summary | 升级为后端 Project 列表和 Research Brief 投影 |
| session-store / workspace-store | 新主链使用按 research/run ID 分桶的数据缓存；旧 store 限于兼容页 |

建议沿现有目录新增/替换职责明确的 `api/research`、`run-store`、`pages/workspace`、`pages/assets` 与简报/阶段结果组件，不一次性重写所有基础 CSS 或医学页面。这里是产品链替换，不是另加一个空 Workspace 壳。

### 5.3 前端状态和视觉规范

- 后端 snapshot 是状态真源，events 是增量解释。UI 不能因定时器、动画结束或 Graph 非空就推进阶段。
- 活跃阶段突出一句“正在做什么”；已完成结果默认折叠长细节。没有可靠分母时不显示虚构的 63% 进度。
- 每次请求捕获 research_id/run_id/revision；迟到响应只更新对应缓存，不写当前研究。路由切换清理订阅/轮询，断线后按游标补齐并去重。
- 主区区分：准备运行、运行中、需要具体决定、暂停/恢复、失败、仅预览完成、真实资产完成、缓存回放。不能用同一绿色完成标签表示全部。
- 资产计数来自服务端 manifest，观测行数来自解析/导出结果；用“已下载 / 已验证 / 待审查”替代模糊成功。
- 继续追问入口常驻；可查看过程但不要求点“下一步”。运行期间可以展开来源/证据、切换项目和只读追问。
- 右侧风险/缺口始终可读；技术字段藏于默认关闭的 Drawer。用户确认过的公开数据限制可用自然语言摘要展示，无需暴露 constraints JSON。
- 视觉以研究文档、来源和表格为主：有限强调色、紧凑活动条、统一字体/间距，不用高密度工程徽章或八步进度墙。
- 桌面保持三栏；窄屏左侧抽屉、Brief 折叠。键盘可操作，焦点不随轮询重绘丢失，错误说明不只依赖颜色。

## 6. 数据资产闭环与科研可信性

### 6.1 四级结果必须分离

| 级别 | 含义 | 可否叫 dataset |
|---|---|---|
| plan | 工作假设与执行合同 | 否 |
| candidate | 尚未验证的来源/字段候选 | 否 |
| preview | 来源/映射/图的预览或待审查结构 | 否；真实抽样预览也必须标示 sample 与实际来源 |
| materialized asset | 真实获取、解析、质量判定、写入且可校验的版本化产物 | 满足数据合同后可；仍需注明质量状态和用途限制 |

Graph 上的“计划使用某字段”和“某观测值来自某列”是不同证据等级。LLM 可以生成计划和带引用的解释，不能生成真实观测、患者标记或缺失测量值来填表。

### 6.2 最小资产包与页面

| 交付物 | 内容与交互 | 验收要点 |
|---|---|---|
| `dataset.csv` | 真实观测/领域记录；列字典、预览、排序过滤、下载 | 非空且与存储记录一致；量纲/粒度明确；不把预览行重复铺满 |
| `metadata.json` | 原问题、目标/合同版本、字段类型/单位、时间与星等系统、redshift frame、缺失政策、来源与引用、执行模式 | 能解释每列是什么、来自哪里、可用于什么；不包含密钥 |
| `quality_report.json` | 规则版本、实际分母/计数、缺列/缺失/重复、join 统计、冲突/隔离行、通过/阻断原因 | 不伪造 SDTI/准确率；没有 Gold Set 的指标显示未评测 |
| `evidence_graph.json` | 带类型/证据等级的 source、raw record、字段映射、转换、观测与资产引用 | 关键字段能回到证据；界面可查看/解释，未知边不显示为事实 |
| `source_lineage.json` | 请求 URL/时间、响应哈希、原始文件、表/行/字段位置、解析/转换/join/导出版本及上下游关系 | 从 CSV 单元格反查到真实下载和 metadata 依赖，不只是一串候选 source 名 |

Asset Center 是同一研究下的数据、元数据、质量、证据、血缘五个视图，不是五个新的 Agent 工作流。资产生成能力在 B/C 建立，E 负责查询、阅读、核验和打包体验。

### 6.3 解决一行多字段、多来源的溯源表达

一个观测行的 magnitude 来自 table6，而 redshift 来自 table1；只给整行一个 `raw_field=mag` 不满足用户要求。

天文观测宽表建议保留目标字段，并加稳定 `record_id`/`provenance_ref`。`source_id` 表示主观测来源；每个输出字段的实际来源放入字段级 provenance。CSV 中要求的 `raw_field`、`raw_value` 使用合法 JSON 映射字符串，按目标字段对齐原字段定位和原始词面值；多输入派生使用数组。Metadata 明确该编码，标准 CSV 解析后可无损恢复，界面默认不展开大 JSON。

`source_lineage.json` 按 record_id/目标字段记录完整信息：source_id、下载对象哈希、表名、原行标识、raw_field、raw_value、解析器版本、转换规则、metadata/join 依赖、质量状态。已有医学冻结格式不改；通过独立 sidecar 关联导出记录来补齐字段级 lineage，不能为天文把医学 CanonicalRecord 改成另一种格式。

未提供的 flux/redshift 等值使用 null/空字段，并记录 missing reason；不写零或字符串“未知”冒充数值。派生值必须标为 derived，保留公式/规则/输入证据；纯标识与缺失状态也要说明生成或缺失依据。

### 6.4 数据质量与发布门

- 原始响应只读，标准化在新版本上进行；清洗不是覆写原值。重复数据以来源和观测身份判断，不能按“同 SN、同时间”就删除不同波段/独立测量。
- 对必需的非空科学字段逐项检查有效证据；目标为全量可追溯。缺证据的字段/记录进入隔离，不靠提高模型 confidence 通过。
- 质量失败可以交付失败报告和审查材料，但不能显示 ready dataset。警告不等于失败，例如来源没有 flux；须与该 Run 合同约定一致。
- 原有医学安全、质量规则及 `docs/06_评测指标与SDTI.md` 公式继续冻结。不得给天文数据套医学量纲，也不得用新增易过规则代替既有门禁。
- 私有工作区内生成可恢复资产可自动进行；对外发布是另一项需确认的动作，还必须满足许可、隐私与科研质量条件。
- 下载文件完成写入、解析验证、checksum 核验后，事务提交 manifest/asset_created；失败时不留下可下载的半文件成功态。

### 6.5 多模态闭环：Parser → Extraction → Validation → Provenance

| 材料 | 实施设计 | 比赛前最小验收 |
|---|---|---|
| 论文/正文 | 真实 PDF 文本层或 JATS/HTML → 段落/章节提取 → 引用定位 | 可回到文件哈希、页码/章节与原文；无文本层时明确需要 OCR/不支持 |
| 表格 | 复用 CSV/TSV/Excel/HTML parser，补实际表结构与类型校验 | 下载表/真实附件能逐值对回原表；表头不是 LLM 编造 |
| 附件 | 获取并保存原始附件，绑定论文与来源、许可信息 | 附件—解析记录—字段—资产链可审计；大小/格式有上限 |
| 图像 | 真实图片输入经过视觉模型/可验证提取，记录坐标轴、图例、图注和不确定性 | 至少一幅真实图可追到页码/图号/区域；只读图注不能标像素已理解；无视觉模型则诚实降级 |
| 图数字化 | 像素标定、线性/对数轴、点提取、不确定度、与原图/原表对照 | 未完成质量验证一律不入主表；完整自动数字化可延后，不展示虚构点 |

图片/论文中的指令是数据，不是可执行 Agent 指令。外链、附件、模型建议经过允许域/工具校验；防止提示注入、任意 URL 取数、任意路径写入。提取失败进入 review，不让生成模型补齐数字。

## 7. Ia 型超新星真实案例路线

### 7.1 使用已验证证据，不把计划当验证

本节数据来自 B0 的实际 HTTP/TSV/VOTable 响应，访问窗口为 **2026-09-05 19:11:04–19:11:41（Asia/Shanghai）**。本轮没有重新访问这些站点；行数是该次快照统计，不是永久保证。正式实现须重跑 live 验证。

| 角色 | 真实来源 | B0 已确认情况 | 使用决定 |
|---|---|---|---|
| Primary | CDS VizieR `J/ApJS/200/12/table6` + 同目录 `table1` | 无认证 HTTP 200；TSV/VOTable；5485 条光变观测、92 个 SN；metadata 94 个对象；92/92 对象 exact SN 匹配 | 首个真实观测级 CSV 的完整 source family |
| Secondary | CDS VizieR `J/ApJ/959/132/table1`，SN 2021aefx/KSP | 无认证 HTTP 200；1056 条观测；行内无 SN/z，ReadMe 给单对象身份和 z | 独立观测资产与 provenance 测试；不与 CfA4 观测硬 Join |

官方入口和机器端点已记录在 `IA_SUPERNOVA_SOURCE_VALIDATION_REPORT.md` 及 `tmp/source_validation/vizier_validation_report.json`。CfA4 是多对象样本；KSP 是另一项科学来源，但两者都由 CDS 托管，不能称为独立基础设施或同一对象的交叉校验。

### 7.2 真实字段合同

| 输出字段 | CfA4 真实输入 | KSP 真实输入 | 强制语义 |
|---|---|---|---|
| sn_id | table6.SN | ReadMe Objects 的单对象绑定 | 来源命名空间与原标识同时保留；不依赖 LLM 名称猜测 |
| observation_time | MJD | MJD | 数值 MJD，单位 d；更细时间尺度未明确，不擅自标 UTC/TDB |
| band | Filt | Band | 原波段字符串保留，不自动合并 r/r′ 等不同定义 |
| magnitude | mag | mag | 单位 mag；星等系统必须在字段/波段 metadata 中声明 |
| flux | 无 | 无 | nullable，不由模型生成或静默转换 |
| error | e_mag | emagTot | 总星等不确定度，单位 mag；不混用 flux error 或仅检测误差 |
| redshift | table1.z 或 zCMB | ReadMe Objects.z（可仅作 metadata） | 明确 frame 与来源；未决定时可留空并保留两种原始 z |
| source_id | 固定已验证的 table6 source binding | 固定 table1 source binding | 元数据依赖在字段级 provenance 单独引用 |
| raw_field/raw_value | 实际原列和原始词面值 | 实际列或 ReadMe 元数据位置 | 每个非空科学字段都有原值或明确派生链 |

KSP 的 B/V 为 Vega、i 为 AB，不能只保存一个全表 magnitude_system。CfA4 的系统定义也需从 ReadMe/原始文献按实际滤镜核实；未确认的标为未明确，不自动统一。观测时间格式 MJD 与时间尺度是不同概念。

### 7.3 redshift 和关联政策

- CfA4：只用 `trim(table6.SN) = trim(table1.SN)`，先验证 metadata key 唯一，再多对一左关联。Join 前后观测行数保持不变；统计对象/行匹配率和未匹配原因。
- 本次快照为 92/92 对象匹配；table1 独有 `2009hp`、`PTF10bjs`，不能制造它们的光变行。`recno` 不是跨表 join key。
- metadata 重复 key 是阻断问题；未匹配或 z 缺失则保留原观测、redshift=null 并报告，不能用名称相似度、星系或模糊坐标凑匹配。
- `z` 是 heliocentric，`zCMB` 是 CMB，保留二者原值。普通光变整理默认不替用户选择宇宙学分析框架；`redshift` 可暂空，或采用已经版本化、可解释且被研究合同选定的规则，不反复强迫用户为普通取数确认。
- KSP 的身份/z 来自 ReadMe Objects，不是另一张观测表。B0 请求候选 table2 虽 HTTP 200 却没有有效数据表，应记语义失败。只可在验证单对象目录约束后传播元数据，保留 ReadMe 位置/哈希；redshift frame 未知时不伪造。
- Secondary 默认单独资产。若未来做 union，必须经过字段、波段/时间/星等系统和对象命名空间兼容检查，并保留 source 分区；union 也不是同对象 join。

### 7.4 B1/B2 实施范围

1. 固定 CfA4 table6/table1 的可验证 binding，检查原始 ReadMe、响应格式与真实表结构；新增 astronomy 能力只在新 Run 域中启用，不把现有 v30 全部 astronomy catalog 标成可取数。
2. 首选带字段元数据的 VOTable；TSV 为可审查备选。两种格式都检查错误信息、预期表、必需列、实际行数、溢出/截断状态；比较规范化记录，不只比较 HTTP 或 XML 字段名。
3. 有限速率、超时、重试、下载/行数预算；对不存在表、HTML 错页、200+错误载荷、重复 metadata key、部分下载编写失败测试。
4. 原始响应 hash 与规范化数据 hash 分开；动态 request_date 不应导致误判科学数据变化。哈希由文件重新计算，以机器验证清单为准，不从 Markdown 手抄摘要。
5. 合同校验后导出真实表、字段级 lineage 和质量报告，再进入新 Run 的资产生成。冻结快照测试可断言 5485/92；实时来源行数变化需核查版本，不能截取/补造到固定数量。
6. KSP 用独立 binding 验证不同字段和 metadata 路径；至少独立取数/解析/预览，完整副资产为 B2。主源不可用时不能把单对象 KSP 冒充原多对象研究完成。
7. 保存原论文、catalog/table 标识、访问日期、VizieR acknowledgement 和明确的数据使用条件。B0 的公开访问/引用说明不等于已确认所有再分发许可；许可待核实时禁止公开发布数据包。

B0 足以支持受限进入 B1，但不证明长期服务稳定性、所有 VOTable 数据行与 TSV 等价、最终质量门或完整发布许可已通过。上述验证属于 B1 必交，不重复造一份“候选可用”文案来替代。

## 8. API Key 配置与真实执行模式

### 8.1 优先复用现有安全机制

现有 `GET /api/agent/configuration`、`POST /api/agent/qwen-sessions`、`DELETE /api/agent/qwen-sessions/{session_id}`、`POST /api/agent/api-check` 可以作为配置基础。`backend/app/agent/session_registry.py` 将凭据保存在进程内，默认 TTL 7200 秒、容量 50；这一机制不等于科研历史持久化，也不等于新 Run 已接入 Qwen。

首次启动读取非敏感配置状态：已配置、未配置、配置失效、服务不可达。未配置时清楚展示“未配置模型”，提供“配置模型”和“受限演示”两条入口，不把某个开发者本机环境当作比赛环境前提。

### 8.2 凭据流

用户只在专用配置表单输入 Key，提交到后端验证后立即清空输入。前端不保存原 Key，不放入 localStorage/sessionStorage、URL、源码、构建产物、埋点、console、Run 事件、错误回显或导出材料。远程部署必须 HTTPS；本机服务仅在明确的本地边界使用。

后端优先使用部署环境的安全凭据；用户临时配置沿用短 TTL 注册表。Run 只接收/解析受控凭据句柄，服务端确认归属和有效期。句柄也是敏感凭据，不写科研资产或长期 Session history；持久 Run 只保存非敏感 provider/model/mode 等审计信息。

模型失效/重启后需要重新配置时显示明确状态；不能恢复成“仍已使用模型”。日志和错误链统一脱敏，允许的服务端点与协议经过校验，不能让任意 base URL 成为 SSRF 通道。

### 8.3 三种模式不能混淆

| 模式 | 能做什么 | 必须展示什么 |
|---|---|---|
| model_assisted + live | 模型参与理解/计划/有证据的解释，工具获取和验证真实数据 | 本次实际 provider/model、真实数据来源和工具结果，不承诺模型生成事实 |
| deterministic + live | 明确支持的规则化研究路径，真实 CfA4 等工具仍可取数 | “未使用模型，受限规则模式”；不支持的自由追问诚实说明 |
| replay + cached_real | 回放带来源、原始响应和哈希的真实历史材料 | “缓存回放”、原采集时间、原 run/asset 标识；不冒充本次联网执行 |

模式分开记录模型策略和数据策略。Fallback 可降级能力，不能降低数据真实性。没有网络也没有可核验缓存时，显示不能获取数据；禁止生成演示 CSV 补场面。演示数据包涉及再分发时仍需先满足许可要求。

## 9. 分阶段实施计划

依赖顺序：先确认本方案与统一契约；A1 加固现有编排，B1 打通真实天文数据；C 完成持续研究与资产持久化；D 接管主入口；E 完成资产中心；F 端到端演示验收。C 的最小模型定义、D 的静态布局设计可以提前并行，但不能用前端临时状态替代 C，也不能把 UI 联调误报为整个阶段完成。

| 阶段 | 实施任务 | 主要交付与依赖 | 明确不做 |
|---|---|---|---|
| A：Orchestrator（在已验收 Phase A 上做 A1） | 可执行合同；完整阶段接口；原子控制、风险门、事件结果级别；Qwen/模式贯通；复用医学执行器并传递真实选择/约束 | Run contract、阶段/事件契约、复现与回归测试；为 B 留真实数据执行入口 | 不推倒现有 Run 服务，不伪造缺失插件结果 |
| B：Ia 真实插件（B0 已完成，进入 B1/B2） | B1 CfA4 真取数/解析/关联/质量/资产文件；B2 KSP 独立路径与多模态材料接入 | 实测来源、真实观测级 CSV、五类资产内容、失败夹具；依赖 A 的阶段合同 | 不制造 flux，不跨源猜 join，不修改医学冻结 schema |
| C：Project/Run/Asset 与历史 | SQLite/文件持久化；消息/记忆版本；项目与 Run 列表；追问派生/恢复/归档；所有权校验 | 可恢复的研究历史、资产 manifest、检查点和迁移；将 A/B 内存结果转为正式归属 | 不把前端 Map/localStorage 当历史库，不默认上分布式队列 |
| D：Autonomous Workspace | 替换 Home/Copilot 主链；三栏、事件叙事、Brief、追问/控制；模型配置；统一根路由和容器 | 真实 API 驱动工作区；两研究并行切换浏览器验收；依赖 A/C，B 数据用于验收 | 不用定时器假推进，不扩建八步页面集合 |
| E：Asset Center | 数据/Metadata/Quality/Evidence/Lineage 五视图；单元格反查；版本与下载/打包 | 可从研究进入资产并回到 Run 的闭环；依赖 B/C/D | 不把 Graph 截图当证据交付，不将下载等同公开发布 |
| F：最终 Demo | 干净环境部署、两域真实链、异常/无 Key/缓存回放、5–8 分钟脚本、回归与交付清单 | 可复演发布包、实际测试结果和缺口清单；依赖全部必交闸门 | 不展示未测性能、虚假指标或离线数据冒充 live |

每阶段单独报告：改动文件、实际运行命令、证据、失败/跳过项、剩余风险；不能以“页面已出现”作为阶段验收。实现时保留现有未提交改动，先确认相交文件，再做范围内变更；回滚以版本化发布/迁移策略进行，不删除旧数据或重置用户工作区。

## 10. 每阶段验收标准

### A 验收：后端自主，而非换了状态文案

- 一句话创建 Run 后不再需要前端调用 planner/discovery/mapping 等阶段 API；断开浏览器仍推进。
- 普通模糊研究自动进入带工作假设的可执行计划；缺少能力时明确停止/仅预览，不无限澄清、不伪造能力。
- 阶段失败后没有后续成功事件；空候选不能发出“已选择来源”；事件序列/结果级别可校验。
- 可在执行中的慢阶段暂停/取消，迟到结果不能覆盖状态；具有恢复/风险决议契约，而不是只有暂停无去路。
- 同领域与跨领域双 Run 的 source binding、schema instance、mapping、memory、graph、事件均隔离。
- “仅公开数据/排除某来源”等限制实际抵达工具调用；医学 IHC/CNA/response_domain/患者关联红线回归通过。
- 配置模型后验证新 Run 真正调用相应模型；未使用模型就明确标记 deterministic。

### B 验收：真实 CSV 与来源回查

- 保存真实请求、响应、HTTP/Content-Type、访问时间、原始 hash；语义校验发现错误表、溢出/截断、缺列。
- 对 B0 固定快照解析 CfA4 5485 观测/92 SN，metadata 多对一 join 不增加观测行；关键原始值逐项对回文件。实时采集另报实际数量与变化原因。
- Csv、Metadata、Quality、Evidence、Lineage 内容全部生成且交叉引用有效；所有关键非空数据字段具有有效 provenance。
- 无 flux 时为空，MJD 不偷换时间尺度，redshift 不混 frame；未匹配/重复 metadata 有实际测试。
- Secondary 能真实独立获取与解析；不把 1056 条 KSP 单对象观测宣传成 CfA4 的跨源对象校验。
- 至少一条论文/正文—表格/附件—图像证据链可定位原材料；图像真实读像素时方可标理解成功，图数字化未验收不得入主表。
- 科学数据来自真实快照/live；异常测试可构造错误响应，但必须单独标为测试 fixture，绝不进入演示数据或正式资产。

### C 验收：历史与上下文是可恢复的数据

- 创建 HER2、Ia 两研究并多轮追问，research/run/session/asset 关系合法；篡改所属研究 ID 的访问/操作被拒绝。
- 刷新浏览器、关闭重开、重启后端后，标题、对话、摘要、Run 历史、事件和已提交资产仍存在。
- 未完成运行恢复为明确 interrupted/可恢复状态；不重复下载/生成同版本资产，不恢复已过期密钥。
- 同项目追问产生新版本或只读回答；旧 Run/CSV 不变；明确跨域扩展使用隔离关联研究。
- 重复提交通过幂等机制不产生双 Run/双资产；归档不等于删除数据。

### D 验收：真实用户完成同一条产品链

- 从正式根地址输入 Ia 问题，只点击一次开始，进入 Research Run；无被迫“选择方向/下一步/继续”。
- 左栏只有研究与历史导航；中栏有原问题、理解、工作假设、计划、真实进展、结果和追问；右栏是 Brief，工程细节默认折叠。
- 延迟 A 响应时切到 B，A/B 的正文、简报、来源、图、输入内容都不串；切回恢复准确。
- 断网、后端重启、过期模型、等待确认、暂停、失败、仅预览、资产完成都有正确状态和可行动说明。
- 浏览器网络记录证明前端不直接串联阶段、不调用不存在 API；页面不因刷新重复启动 Run。
- FastAPI 直启与构建后的 Nginx 容器显示同一主产品；旧入口明确隔离。键盘、窄屏和长结果可用。

### E 验收：不是只有下载按钮

- 五类资产可查看与下载，下载文件与 manifest 的大小/hash/版本一致；重新解析 CSV 的行数和字段与页面一致。
- 任取一条观测的 magnitude 和 redshift，分别定位观测表和元数据表的原行/原字段/原值及 join 规则。
- 可以从 Quality 中查看缺失、隔离和未匹配记录；从 Lineage 看到真实下载而非候选目录；从 Graph 解释证据等级。
- 草稿、失败、缓存和正式资产标识准确；不同研究/不同 Run 版本不混用；无数据时不得导出“成功 CSV”。
- 导出包无凭据和无权传播的数据；发布另需明确授权和许可门。

### F 验收：比赛可复演

- 干净部署环境完成模型状态检测、配置、真实运行和受限无模型模式；没有开发者本机隐式配置依赖。
- Ia 从一句话到真实资产全链打通；HER2 在新 Workspace 中真实复用既有医学链，不能只跳转回旧页面。高风险患者关联留在审查，不要求为了演示而合并。
- 5–8 分钟脚本覆盖：输入 Ia → 后端自主进展 → 切换另一研究 → 解释来源 → 展示真实 CSV/质量 → 单元格溯源 → 继续研究/历史版本。
- 运行耗时现场实测；若超出演示时长，用明确标为先前真实完成的资产辅助讲解，同时显示当前 Run 的真实状态，不把速度写进动画。
- 执行原医学固定回归集、v30、自主 Run、来源/资产、持久化、安全及浏览器 E2E。固定基线集合见 `docs/v31_baseline/REGRESSION.md`；旧产品形态测试经审阅替换，安全断言保留。真实外网测试单列日期/结果，跳过不算通过。
- 交付实际通过/失败/跳过清单，不沿用旧报告数量或虚构 SDTI；冻结文件哈希、发布文件清单、备份/回退路径明确。

## 11. 风险分析与控制

| 风险 | 影响 | 处理与退出条件 |
|---|---|---|
| 一次重写所有页面和所有 Agent | 时间失控、旧能力回归 | 以统一 Run/资产契约组织纵向交付，复用内核；不强制换框架 |
| 只接 Phase A 预览就宣称成品 | 比赛无法展示真实数据价值 | B 真数据和 E 字段级回查为硬门，preview 永不冒充 asset |
| CDS 网络/限速/表版本变化 | 无法稳定现场下载 | 有界重试、校验缓存、记录访问时间；同服务两个目录不是容灾双源 |
| 公开可访问被误当可随意发布 | 数据使用边界不清 | 保存原始许可/引用信息；未核实再分发条件则关闭公开发布 |
| z/frame、星等系统、时间尺度混用 | 产生科学上不可比较的表/结论 | 源字段与元数据优先，未知保持未知，变换规则单独审核 |
| 患者 Join 与天文 Join 共用宽松策略 | 医学身份/语义错误 | 领域分离，医学冻结规则不可由用户或模型豁免 |
| 暂停/取消竞态与任务共享对象 | 结果串线或取消后仍完成 | revision/事务、Run 专属 binding、检查点及延迟响应测试 |
| 持久化后漏存 memory/Brief/产物文件 | 有历史标题但无法继续 | 重启恢复覆盖所有对象及文件引用，不只检查研究列表 |
| 图注分类包装成多模态 | 能力声明与实现不符 | 实际像素/文件证据验收；未支持清楚标注，数字化不偷入主表 |
| 模型凭据泄漏、恶意材料指令 | 账户/数据安全风险 | 短期凭据、脱敏、受控工具/外链、来源内容不授予执行权限 |
| 前端/容器仍有两个正式入口 | 演示展示旧产品 | 根路由、静态资源、部署和 E2E 一起切换 |
| 测试全绿但契约仍是旧阶段限制 | 错把旧 UI 保持不变当验收 | 新增用户路径/并发/真数据/文件验收，保留安全基线 |

完整自主研究不等于保证每个问题都能产出 CSV。没有公开数据、能力不支持、关键科学语义不明或风险不可解时，应返回可追溯的研究进展、缺口与下一条可行路径，而不是制造数据填满交付物。

## 12. 比赛前必须完成与可延期内容

### 12.1 必须完成的发布清单

- [ ] 一个正式 Workspace 入口；从一句话直接进入后端自主 Run，无普通阶段连续确认。
- [ ] Phase A 运行控制/状态一致性、执行模式、风险动作门禁得到真实测试；不是仅增加阶段名称。
- [ ] CfA4 真实观测数据和确定性 metadata 关联；Secondary 独立取数验证；错误来源能被语义识别。
- [ ] 五类真实资产可下载并逐字段回查；主表不含伪造观测或无依据 redshift/flux。
- [ ] HER2 与 Ia 两领域在新工作区可运行、可切换；医学规则与高风险审查不退化。
- [ ] Project/Run/Session/Asset 及对话、memory、Brief、历史持久化；刷新、服务重启、迟到响应不串研究。
- [ ] “为什么选来源”的有证据解释，以及至少一种约束修改/方向继续产生新 Run 的闭环；旧资产不覆盖。
- [ ] 论文、正文、表格、附件、图像至少形成可验证的最小材料链；图理解真实/降级边界清楚，未验收数字化不进入主表。
- [ ] 安全模型配置和未配置状态；真实规则模式/缓存回放均有显著标签，绝不混充 live+model。
- [ ] 单一部署产物、医学与新链回归、浏览器 E2E、5–8 分钟实测 Demo 和来源/许可/限制说明。

以上是建议的比赛发布门，不是本轮已经完成的勾选结果。若期限不足，应明确缩减支持范围并披露，不能把缺失能力换成成功动画。

### 12.2 可以延期，但不能虚称已支持

任意领域自动接入、通用网页爬虫/插件市场、大规模分布式调度、多租户协作、跨源天体自动消歧、完整光变拟合/因果推断、自动图数字化入表、任意 PDF OCR、公开发布平台、SSE/WebSocket 和全前端框架迁移。

KSP 完整第二数据资产可在 Primary 稳定后推进；比赛前至少有真实独立解析证据。图像数值提取可延期，但若真实图像理解也未实现，必须列为赛题多模态能力缺口，不能以 caption 关键词标签代替。

### 12.3 本轮自检与待确认结论

本轮已重新核对核心前端入口/路由/store、v31 编排与测试、v30 候选/schema/图/医学委托、解析/导出和模型凭据设计，复核 B0 保存的验证材料，并完成 135 项专项回归。

本助手本轮仅新增 `FINAL_PRODUCT_REFACTOR_PLAN.md`。未修改业务代码、冻结配置、旧报告或既有用户改动；未编写正式 Adapter、新增接口、生成科学数据或样例 CSV。三份冻结配置和 SDTI 公式文档在本轮文档写入前后的 SHA-256 一致；工作区其他差异不能归入本方案交付。

建议确认本方案后，从 **A1 运行契约与控制加固 + B1 CfA4 真实资产纵向链** 开始实施，同时确定 C 的最小持久模型。D 必须接入这些真实结果并替换默认主链；不再单独开展一轮 Copilot 卡片修补。
