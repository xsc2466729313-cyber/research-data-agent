# 给 Codex 的第一条消息

请把当前目录视为新项目的需求与约束启动包。

开始前：
1. 阅读 `AGENTS.md`。
2. 按 `README_START_HERE.md` 指定顺序阅读 `docs/`。
3. 阅读 `configs/` 和 `schemas/`。
4. 不要修改冻结 Schema 和医学规则。

然后执行 `prompts/00_初始化仓库.md`。

只完成阶段 00，不要提前实现真实 GDC/GEO/cBioPortal/AACT/CIViC Adapter。

阶段 00 完成后：
- 运行所有测试；
- 汇报新增/修改文件；
- 汇报测试结果；
- 列出下一阶段建议；
- 若认为冻结接口必须修改，创建 `CHANGE_REQUEST.md`，不要自行修改。
