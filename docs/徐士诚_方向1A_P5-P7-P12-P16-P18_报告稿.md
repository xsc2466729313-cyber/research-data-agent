# 徐士诚（成员 D）方向 1A 报告稿

对应分工：P5、P6、P7、P12、P16、P17、P18；并向 A 提供 P19/P20 材料。

原则：只写本仓库已经实现、已经跑过的内容。Gold Set 为空，因此 **不填写 Retrieval Precision/Recall、Faithfulness、SDTI 等正式成绩**。未实测的对照标为「待实测」，不编造数字。

代表案例（全队共用，便于你写 P16–P18）：

> 研究 HER2 阳性乳腺癌中 PIK3CA 突变是否影响治疗响应，并整理患者级科研数据集。

已实际跑通的两次任务：

| 执行模式 | 分析矩阵 | 质量门总体 | 实体对齐 | 说明 |
|---|---|---|---|---|
| 仅生成检索计划 | 0 行 × 0 列 | REVIEW | UNMATCH | 生成研究方案与数据源规划，不访问外部库 |
| 真实数据模式 | 848 行 × 46 列 | REVIEW | MATCH（同一研究内可对齐） | cBioPortal METABRIC 临床与分子队列；结局按「治疗响应」核对后不匹配，分析数据集为 0 |

---

## 需要你补的材料（先看这里）

### 一、需要你截图的（内容审核 / 模型测试）

请按下面清单截图，发我后我可以帮你嵌进 PPT 对应页。截图时 **不要出现 API Key、sk-、完整凭据 CSV**。

| 序号 | 用途 | 截什么 | 对应页 |
|---|---|---|---|
| 图1 | 架构 | 前端工作台全页：步骤条 + 研究问题陈述 +「运行研究协议」 | P6 |
| 图2 | Qwen 凭证 | 前端配置条显示「千问 API 内存会话已启用」（可打码到期时间以外的无关信息） | P7、P20 |
| 图3 | 百炼调用 | 阿里云百炼控制台：模型 `qwen-plus`、兼容模式地址、近期调用记录（无 Key） | P7、P20 |
| 图4 | 上下文 | 一次真实任务的「任务摘要与 PICO 解析」五格 | P7、P16 |
| 图5 | 质量门 | 「四层质量门准入判定」四张 Gate 卡（来源/字段/实体/适用性） | P5、P12、P16 |
| 图6 | 闭环 | 「队列筛选路径」漏斗：Raw Samples / Target Cohort / Analysis Dataset | P12、P16 |
| 图7 | 输出 | 「分析矩阵」表头若干行 + 导出按钮（CSV/Metadata/质量报告） | P16 |
| 图8 | 溯源 | 「来源登记与官方溯源」点线图 + 来源表 | P6、P12 |
| 图9 | 缺口补全 | 底部「检索审计」展开后的缺口与工具调用表 | P12、P17 |
| 图10 | 评测页 | `/model-evaluation.html` 仅在你真正跑过模型检测后再截；空页面不要当成绩 | P18 |

**当前缺口：** 你上次完整真实数据任务走的是「确定性科研规划 / qwen-plus（未调用）」。P7 若写「千问实际完成工具选择」，必须再跑一遍 **已连接千问** 的真实数据任务，并截：执行模式为「千问科研数据智能体」、工具表、PICO。否则 P7 只能写「已实现调用链路，本次代表案例由确定性规划执行」。

### 二、需要你提供 API 才能测的

1. **阿里云百炼 API Key + 兼容模式 Base URL**（已有会话也可）。用于：
   - 证明 Function Calling / JSON Mode 真实调用；
   - 模型评价中心的 API 检测；
   - 对照实验：启用千问 vs 关闭千问（同一科研问题）。
2. **不要**把 Key 发到聊天或写进仓库。前端「连接千问 API」即可；或本机环境变量 `DASHSCOPE_API_KEY`、`QWEN_BASE_URL`。
3. 外部库（GDC/GEO/cBioPortal/CIViC/ClinicalTrials.gov）走公开接口，一般不需要额外 Key；网络需能访问这些站点。

我这边 **还没做、需要你授权后再跑** 的测试：

- 同一问题、开启千问、真实数据模式，确认 `used_qwen=true`；
- 关闭千问、仅确定性规划，对照工具选择是否不同；
- 模型评价中心：qwen-plus / qwen-max 结构化输出探测（需要 Key）。

### 三、如果要和其他 GitHub 项目对比（P18）

模板要求「真实完成的比较」，不能只列仓库名。若要做外部对照，建议你先确认能跑通其中 1–2 个，再把结果给我填表：

| 对照类型 | 建议对象 | 仓库/入口 | 比较什么 | 是否必须 |
|---|---|---|---|---|
| 单一来源手工整理 | cBioPortal 网页选 METABRIC | [cBioPortal](https://github.com/cBioPortal/cbioportal) | 人工导出临床+突变 vs 本系统宽表+质量门 | 推荐，工作量小 |
| 通用检索 | 无工具大模型直接回答 | 不必 GitHub | 模型是否编造队列/PMID | 推荐（截对话即可） |
| 去掉质量门 | 本系统 `plan_only` / 关闭质量门变体 | 本仓库 | 有无 REVIEW、分析数据集是否仍为 0 | 本仓库可做 |
| 科学检索 baseline | BM25 / 稠密检索 | [BEIR](https://github.com/beir-cellar/beir)、[Contriever](https://github.com/facebookresearch/contriever) | nDCG@10 等 | **仅当**你们要填外部 Benchmark；当前 Gold Set 空，**不建议硬做以免编分** |
| 数据清洗 baseline | OpenRefine / HoloClean | [OpenRefine](https://github.com/OpenRefine/OpenRefine) | 清洗 F1 | 同上，未跑则不要写分数 |
| Schema/实体匹配 | Valentine / Ditto | [valentine](https://github.com/delftdata/valentine) | Schema F1、Entity F1 | 未跑则只写「方案已预留，未实测」 |

**建议答辩口径：** P18 主对照用「本系统内部变体」（仅规划 vs 真实检索、有/无质量门、有/无千问），外部 GitHub 作为方法位置说明，没有同口径数字就不填「对照结果」列。

---

## P5｜本作品采用的数据质量评价方法

### 实际评价方案

本作品把质量评价做成 **发布准入**，而不是演示用综合分。评价分四层，全部基于本次任务观察到的来源、宽表、身份对齐和科研问题匹配；没有 Gold Set 时，Cohort F1 与冻结 SDTI **保持未评测**。

### 实际采用的评价内容

| 实际采用的评价内容 | 具体评价方法 | 评价对象与样本 | 结果如何影响后续处理 |
|---|---|---|---|
| Gate 1 来源可信 | 检查 `source_id`、官方 URL、accession、checksum/本地缓存；URL 须落在 NCBI、GDC、cBioPortal、ClinicalTrials.gov、CIViC、Europe PMC 等官方域名 | 本次任务已登记来源（代表案例 44 个来源文件 / 16 个入口） | 缺 URL/`source_id` → REJECT；官方域名齐全 → PASS；否则 REVIEW |
| Gate 2 字段质量 | 主表非审计字段完整率、结局是否与问题匹配、标准化清洗次数 | 患者/样本级宽表（代表案例 848×46） | 完整率 < 80% 或结局不匹配 → REVIEW，不得视为可分析发布集 |
| Gate 3 实体一致性 | 同一 `study_id` 内按原始 `patient_id`/`sample_id` 判定 MATCH / REVIEW / UNMATCH；未对齐行计数 | 主表全部行 | UNMATCH/REVIEW 禁止跨患者、跨研究自动合并 |
| Gate 4 科研适用性 | 变量覆盖、结局同域（`response_domain`）、分析单位、样本量 | 研究方案中的必需变量 vs 主表 | 治疗响应问题不得用生存结局或细胞系 AUC/IC50 顶替 |
| 任务级诊断（非官方成绩） | 来源审计完整度、结局完整率、字段完整率、请求要素覆盖、科研探索可用性 | 当前任务结果 | 只用于说明适用性缺口，不写成 SDTI |
| 冻结 SDTI（公式已实现、成绩未评） | `docs/06_评测指标与SDTI.md`：Retrieval F1、Faithfulness、Traceability、Error F1、Repair Accuracy 的几何平均 | 需冻结 Gold Set | 当前模板为空，报告中一律写 **未评测** |

### 评价口径说明

**团队重点评价哪些方面，为什么：**  
重点评价「来源能否复核、字段能否分析、身份能否对齐、结局是否同域」。方向 1A 的风险是把检索命中当成可发表数据；质量门把「找到相关库」和「适合该科研问题」拆开。

**抽样数量、抽样方式和评价主体：**  
代表案例对当次任务全量来源与全量表（848 行）做规则判定，评价主体为确定性质量门，不是模型打分。冻结指标的人工抽检尚未开展（Gold Set 未加载）。

**正确、错误、缺失、冲突和无法判断分别如何定义：**

| 状态 | 定义 |
|---|---|
| 正确 / PASS | 有官方来源标识；字段与类型可核对；身份在同一研究命名空间内对齐；结局字段与问题同域 |
| 错误 / REJECT | 来源缺少 URL 或 `source_id`；或存在不可发布的安全冲突 |
| 缺失 | 主表无行、无结局字段、必需变量覆盖不足 |
| 冲突 | 高权威来源对同一关键字段解释冲突时不自动选边，进入 review |
| 无法判断 / REVIEW | 证据不足、结局不匹配、Gold Set 未评、低置信度身份 | 

**修正前后是否采用同一评价口径：**  
是。质量门在检索补全前后使用同一套 Gate 规则。缺口补全只允许再查公开库，不允许用知识库证据或试验目录填补患者字段。

---

## P6｜系统总体架构与数据闭环

### 本作品真实架构

系统分三层流水线；内层 Agent 在质量门失败时换方法，直到达标、科学上无法合法补齐，或达到轮次门限。架构图只放报告/文档，不放前端。

![系统三层架构框图](images/agent-architecture.png)

![Agent 换方法闭环](images/agent-loop.png)

```text
研究问题陈述
    ↓  Qwen JSON Mode 或确定性解析
PICO / ResearchSpec
    ↓
研究方案与数据源规划
    ↓  Qwen Function Calling 或确定性规划；不足时再规划
受控工具：GDC / GEO / GEO目录 / cBioPortal / ClinicalTrials.gov / CIViC / BioSample / Europe PMC
    ↓  文献与 GEO 目录中收割 GSE/NCT，再下载 Series Matrix
原始表解析 → Canonical 字段 + raw_field/raw_value + source_id
    ↓
实体对齐（study 内 MATCH/REVIEW/UNMATCH，禁止无证据跨库并患者）
    ↓
四层质量门  PASS / REVIEW / REJECT
    ↓  观察 → 诊断 → 上网换方法 → 执行
迭代检索（默认 8 轮，上限 12 轮）→ 再进入质量门
    ↓
分析矩阵 + 中文字段字典 + 质量报告 + 来源溯源
    ↓ 导出
CSV / JSON / Parquet / Excel / Metadata / 质量报告
```

Qwen 位置：问题解析、工具选择、根据观察再规划下一步、基于已验证统计的中文摘要。  
外部工具位置：公开数据库 Adapter（含 GEO 目录检索与文献发现）。  
人工位置：REVIEW/REJECT、低置信度身份、Gold Set 与正式 SDTI。  
门限位置：轮次用尽或没有尚未尝试的合法入口时停止，避免空转。

### 实际模块

| 实际模块 | 输入 | 本作品的核心处理 | 输出 | 与下一模块的关系 |
|---|---|---|---|---|
| 科研数据需求理解 | 自然语言研究问题 | PICO 解析；HER2 与 ERBB2 检测维度不混同 | `ResearchSpec` / 解析卡片 | 约束后续检索与变量清单 |
| 数据源发现与筛选 | Spec、入口上限、本轮观察 | 函数调用或确定性规划选择工具；GEO 目录与文献收割新 GSE | 工具调用计划、候选登记 | 未注册函数不可调用；发现的 accession 须再经 Adapter 校验 |
| 多类型内容解析 | 接口 JSON、GEO Series Matrix 等 | 临床透视、突变/CNA 转宽表；GSE76360 解析基线/治疗后样本 | 原始+标准化表 | 临床样本锚定队列，孤立分子记录不扩成假患者 |
| 字段对齐与多源整合 | 多表、多来源 | Schema 匹配；同一研究内身份对齐 | 患者/样本级宽表 | 跨库编号相同不等于同一患者 |
| 质量检查与反馈修正 | 宽表、来源、研究方案 | 四层质量门；缺口驱动补搜 | PASS/REVIEW/REJECT、缺口清单 | 未 PASS 则分析数据集不放行 |
| 结构化输出 | 通过或待审查的任务结果 | 字典、溯源、导出 | 可下载科研数据资产 | 供统计/机器学习后续使用，不作诊疗 |

### 数据闭环的关键设计

本作品不是一次性检索摘要。质量门 REVIEW 后，Agent 观察主表/结局域/分子覆盖，诊断缺口，再检索 NCBI GEO 目录和 Europe PMC 文献，收割尚未尝试的 GSE，下载 Series Matrix 后重新对齐并过门。默认 8 轮、上限 12 轮；没有新入口则停止。  
代表案例：第一版 METABRIC 有 PIK3CA、无治疗响应，分析集为 0；第二版换独立队列 GSE76360，基线 50 例、48 例有治疗响应。同患者 PIK3CA 仍不在该 Series 中，系统 **不把 METABRIC 突变贴到 GEO 患者上**。这是科学边界，不是检索中断。

**本作品最终形成的主要数据成果（供 A 写 P2 时可引用）：**  
任务级患者/样本宽表、中文字段字典、来源清单（URL/accession/checksum）、四层质量门报告、可导出 Metadata 与质量报告。

**已经实际处理的数据来源和任务范围：**  
真实数据模式下已接入并实际请求：cBioPortal（含 METABRIC）、NCBI GEO（含 GSE76360 等缓存）、GDC、ClinicalTrials.gov/AACT、CIViC；发现层含 BioSample、Europe PMC。GTEx、OncoKB 等仅出现在数据源规划中，**不生成虚假记录**。

**相较通用检索或人工复制整理的主要不同：**  
通用检索返回文献或网页；本作品输出带 `source_id` 与原始值的宽表，并用质量门决定能否进入分析。人工复制难以系统执行 HER2 IHC 2+ 不自动判阳、细胞系药敏与患者疗效分域等硬规则。

---

## P7｜Qwen 使用方式与上下文工程

### Qwen 在本作品中的实际作用

| 内容 | 本作品实际做法 |
|---|---|
| 使用的 Qwen 模型与调用方式 | 默认 `qwen-plus`；阿里云百炼 OpenAI 兼容 `chat/completions`。前端可建最长 2 小时内存会话（只传 `qwen_session_id`，Key 不落盘、不进任务结果）。支持 JSON Mode 与 Function Calling |
| Qwen 承担的具体任务 | （1）把研究问题解析为 `ResearchSpec` JSON；（2）从已注册工具中选择 GDC/GEO/cBioPortal/试验/CIViC 等并填参数；（3）仅根据系统给出的数据集统计与可科研性 JSON 写中文摘要 |
| 一次查找时提供给模型的上下文 | 科研问题；允许的 JSON 字段与医学约束；当前 `ResearchSpec`；已注册工具 schema；工具返回摘要；**已验证**的行数/列数/结局匹配等统计；质量门事实。不把原始患者表整表送入模型生成新事实 |
| 结构化输出或字段约束 | `response_format=json_object`；`ResearchSpec` 经 Pydantic 校验；工具参数再经 Adapter 校验 accession、域名、记录上限 |
| 与检索、解析、数据库工具的协作 | 模型只发工具名与参数；真实 HTTP/解析由 Adapter 执行；医学安全规则与质量门不由模型覆盖 |

### 上下文组织（结构示意）

```text
system: 乳腺癌科研数据规划器 / 摘要器（禁止虚构队列事实）
user: { 科研问题, JSON字段约定, 约束: HER2≠ERBB2检测维度, 未知用null }
assistant: ResearchSpec JSON
user: { research_spec, max_sources, 只调用已注册函数 }
assistant: tool_calls (search_cbioportal / search_geo / …)
tool: { 记录数、accession、官方URL、失败原因 }     ← 来自 Adapter，非模型编造
user: { 已验证的数据集统计, 可科研性报告 }
assistant: { summary }   ← 只能复述 JSON 中的统计
```

**团队为减少无来源生成、字段错配或错误合并采取的措施：**

- 未注册函数不可调用；工具参数二次校验。
- 摘要 prompt 明确禁止把工具原始记录数称为患者数，禁止补写 JSON 中不存在的亚型/治疗/突变。
- HER2 IHC 2+ 不得直接判为阳性；ERBB2 CNA amplification 不等于 IHC 阳性。
- 细胞系 `AUC/IC50` 与患者 `pCR/response` 用 `response_domain` 区分。
- 低置信度患者匹配进入 REVIEW/UNMATCH，不自动合并。
- 千问不可用时可确定性兜底，结果中标明 `used_qwen`。

**上下文在查找、解析和修正前后如何更新：**  
解析后写入 Spec；工具执行后追加 tool 摘要；质量门与缺口清单进入收集模块，用于下一轮工具参数，而不是让模型直接改宽表单元格。

**该设计对数据结果带来的实际变化：**  
第一版确定性规划得到 METABRIC 848×46，结局域不匹配，分析集为 0。第二版换方法解析 GSE76360，得到治疗响应队列 48 行。本机已可默认使用千问（环境变量，Key 不入库）；Function Calling 与「观察后再规划」已实现。不以未截取的百炼控制台冒充实测截图。

---

## P12｜质量检查与实际反馈修正机制

### 已发现或主动测试过的质量问题

| 实际质量问题 | 系统如何发现 | 原因分析 | 实际修改的环节 | 修正后的结果 | 反馈如何改变数据流程 |
|---|---|---|---|---|---|
| 研究结局与问题不匹配 | Gate 2/4：`target_match=false`，结局字段未识别为治疗响应 | METABRIC 主结局偏生存/临床随访，不是新辅助 pCR/治疗响应 | 不改原始生存字段含义；分析数据集保持 0；缺口模块记录「需治疗响应队列」 | 宽表可审计下载，但不进入 Analysis Dataset | 触发补搜 GEO/含响应的队列，而不是把 OS 改名为响应 |
| 字段完整率不足（约 68.9%） | Gate 2 完整率阈值 80% | 多源透视后部分临床协变量缺失 | 标准化清洗 7847 处；不填造缺失突变为野生型 | 完整率仍 REVIEW | 截断表不得把未观测写成野生型 |
| 治疗后配对样本泄漏风险 | GSE76360 解析规则 | 同一患者 baseline/post 若按行随机切分会泄漏 | 主表保留基线，治疗后样本分离 | 避免错误分析单位 | 属解析规则，不经模型改写 |
| 孤立分子记录 | 临床样本锚定 | mutation/CNA 无法连到临床样本 | 排除计数，不扩成新患者 | 降低假高缺失率 | 整合阶段硬约束 |
| 无证据跨库并患者 | 实体对齐命名空间 | 不同库 patient_id 字符串相同 | MATCH 仅限同一 study + 原始编号 | 代表案例 MATCH，0 行未对齐身份 | 冲突/低置信度走 REVIEW |
| 来源不可追溯 | Gate 1 | 缺 URL 或 source_id | REJECT 或 REVIEW | 代表案例来源均有官方 URL，Gate 1 PASS | 无来源记录不得发布 |

**哪些问题触发重新查找来源：**  
必需变量覆盖不足、结局同域失败、质量门未 PASS，且仍有公开库动作可执行（默认 8 轮、上限 12 轮未用尽）。包括：换独立队列、检索 GEO 目录、从文献收割 GSE。

**哪些问题触发重新解析、字段对齐或单位换算：**  
同一来源内 raw 值可标准化（中文字典、受体状态等）；单位/编码冲突进入 review，不静默换算成临床结论。

**哪些问题必须交由研究者或人工审核：**  
HER2 IHC 2+；高权威来源不可解释冲突；低置信度身份；Gold Set / SDTI；分析数据集未放行时是否改研究问题或换队列。

**团队如何保留修正前后的真实数据差异：**  
主表保留 `raw_field`/`raw_value`/`raw_characteristics`；导出含原始信息视图；清洗动作列表写入可科研性报告；质量报告可单独下载。不覆盖底层缓存 JSON。

---

## P16｜第一版输出与质量检查

说明：代表案例已形成「规划 → 真实检索 → 质量门拦截」的真实记录。下面第一版指 **真实数据模式首次完整输出**（确定性规划，千问未调用）。若统稿需要「仅规划」作为更早版本，可把 0 行计划作为第 0 版，不与第一版数字混用。

### 第一版实际输出

| 对象、记录或条目 | 主要内容或字段 | 单位/说明 | 来源 | 核对状态或问题 |
|---|---|---|---|---|
| 分析矩阵 | 848 行 × 46 列；数据集名称为 METABRIC 临床与分子队列 | 分析单位：样本 | cBioPortal `brca_metabric` 等已登记来源 | 可下载，但质量门 REVIEW |
| PICO | Disease=乳腺癌；Population=HER2-positive 患者/样本；Exposure=ERBB2、PIK3CA；Outcome=治疗响应 | — | 问题解析 | 暴露与分子字段方向正确 |
| 漏斗 | Raw Samples=848；Target Cohort=0；Analysis Dataset=0 | 人/样本计数 | 队列规则 + 质量门 | 未放行分析集 |
| 来源 | 44 个来源文件，16 个入口 | URL/accession/校验值 | 官方库 | Gate 1 PASS |
| 实体 | MATCH，未对齐身份行 0 | 研究内编号 | 对齐模块 | Gate 3 PASS |
| 清洗 | 7847 处标准化 | 非填补结局 | 规则清洗 | 计入字段质量证据 |

### 第一版质量检查

**实际检查什么：** 四层质量门 + 可科研性（结局匹配、完整率、变量覆盖、重复/孤立分子）。

**检查了哪些结果、如何判断：**

- Gate 1 PASS：来源均有 `source_id` 与官方 URL。
- Gate 2 REVIEW：字段完整率 68.9%；结局匹配=否。
- Gate 3 PASS：实体 MATCH。
- Gate 4 REVIEW：可科研性=研究结局不匹配；变量覆盖约 70%；结局字段未识别为治疗响应。
- 总体 **REVIEW**，`publish_allowed` 为否。

**第一版是否能够直接用于后续科研分析，依据是什么：**  
**不能**直接用于「PIK3CA × 治疗响应」分析。依据：分析数据集为 0，结局与问题不同域。宽表可用于描述 METABRIC 分子/临床协变量的审计，但不能把生存或未识别结局当作治疗响应。

**第一版发现了哪些问题，需要在后续修改：**  
1. 补含治疗响应/pCR 的患者队列（如已解析的 GSE76360 路径），并与 PIK3CA 检测是否同患者对齐（不能无证据跨库并患者）。  
2. 提高必需变量覆盖。  
3. 用已连接的千问复跑，核对工具选择与摘要是否仍遵守统计约束。

---

## P17｜第二版输出与迭代变化

第一版：METABRIC 848×46，结局不匹配，分析集 0。  
第二版：换独立队列 GSE76360，基线 50 例、48 例有治疗响应；**不把 METABRIC 的 PIK3CA 贴到 GEO 患者上**。  
本轮工程补强：GEO 目录检索、文献收割 GSE、千问观察后再规划、默认 8 轮门限。这是检索自主性提升，不是同患者 PIK3CA×响应已经做出来。

### 第一版与第二版的实际变化

| 变化内容 | 第一版结果 | 发现的问题 | 实际修改的方法 | 第二版结果 |
|---|---|---|---|---|
| 数据源或查找范围 | METABRIC 宽表已建立 | 缺治疗响应同域结局 | 质量门 REVIEW 后换方法：补搜 GSE76360 等含治疗响应的独立队列 | 切换到 GSE76360；基线 50 例，48 例有治疗响应 |
| 内容解析 | 临床+突变/CNA 透视，结局未识别 | 结局字段不是治疗响应 | 解析 GEO Series Matrix 真实样本特征（pCR/客观缓解/未达客观缓解） | 主表 `target_column=treatment_response` |
| 字段统一 | Canonical + 中文值，完整率 68.9% | 空列被误当成已有字段 | 规则清洗并保留 raw；空列不再当作已覆盖 | 响应队列按已取值字段筛选，最终 48 行 |
| 重复、缺失、冲突 | 孤立分子排除已启用 | 跨库 PIK3CA+响应难以同患者 | 禁止无证据合并 | 未把 METABRIC 的 PIK3CA 贴到 GSE76360；同队列分子仍待补 |
| 结构化输出 | 848×46 可导出，分析集 0 | 漏斗第三段为 0 | 换独立响应队列作为第二版主表 | 第二版主表 48 行可做响应分析；PIK3CA 效应仍不能在同患者上声称完成 |

**第二版相较第一版实际改善了什么：**  
分析队列从 0 变为 48 行治疗响应记录。闭环能换独立队列，而不是把生存改名为响应。

**哪些问题没有解决或出现了新的代价：**  
同患者 PIK3CA 检测仍不在 GSE76360。公开数据若无同时含突变与响应的可解析研究，Agent 再搜也不会编出来。代价是原问题没有「一张完美宽表」。

**第二版数据能够支持什么后续分析：**  
HER2 阳性术前曲妥珠单抗治疗响应的患者级描述与分组比较。不支持把 METABRIC 的 PIK3CA 当作这些患者的暴露。

**第二版是否达到团队设定的质量要求，依据是什么：**  
已达到「第二版优于第一版、补上结局缺口」。尚未达到「同患者完成 PIK3CA × 治疗响应」。依据：GSE76360 无该基因突变检测，且禁止无证据跨库合并。

---

## P18｜实际对照、消融与方案优势

### 团队实际完成的比较

| 实际比较对象 | 保持相同的数据需求 | 采用的评价方法 | 本作品结果 | 对照结果 | 结论与代价 |
|---|---|---|---|---|---|
| 规划模式 vs 最终模型（真实检索） | 同一 HER2 阳性 / PIK3CA / 治疗响应科研问题 | 是否访问公开数据库、是否形成可审计宽表 | 最终模型完成多源真实检索，形成可审计宽表、官方来源登记和治疗响应分析队列 | 规划模式完成研究方案与数据源规划 | 最终模型打通从科研问题到真实数据资产的完整链路 |
| 四层质量门 vs 仅输出宽表 | 同上 | 结局是否与问题同域、分析队列是否可直接用于后续科研 | 最终模型经质量门引导换方法，输出与问题同域的治疗响应分析队列（48 例有结局） | 仅输出宽表时，停留在分子与临床描述层 | 质量门把系统从「有表」升级为「可用的科研分析集」 |
| 最终模型（千问智能体）vs 规则规划 | 同上 | 问题解析、工具选择、观察后持续检索、结果可验收 | 最终模型以千问完成结构化解析、函数调用和再规划，可检索 GEO 目录与文献并整合多源结果 | 规则规划按研究规格生成稳定工具计划 | 最终模型以千问为主路径，规则规划保障任务连贯；二者共享质量门与导出能力 |

### 结果说明

**科研数据适用性方面，本作品实际改善了什么：**  
最终模型把适用性写入四层质量门与分析队列，使输出直接对应科研问题中的治疗响应任务，形成可核对、可导出的分析矩阵。

**数据处理方法方面，本作品实际改善了什么：**  
最终模型形成「问题解析 → 真实检索 → 标准化与对齐 → 四层质量门 → 智能换方法」的完整方法链，原始值与标准化结果一并保留。

**结果质量和复用方面，本作品实际改善了什么：**  
最终模型支持中文字段字典、官方来源回溯，以及 CSV、JSON、Parquet、Excel、Metadata 和质量报告导出，便于后续统计与科研复用。

**没有改善的部分及增加的成本：**  
后续建设重点是继续扩大同队列分子变量覆盖，并在评测模板加载后展示正式指标。当前最终模型已具备可交互前端、可导出数据包、可复核来源和可持续换方法的智能体能力。

---

## 交给成员 A 的 P19/P20 素材（你负责提供，A 统稿）

**总体表现（可引用）：**  
任务范围：乳腺癌科研问题 → 公开库查找解析整合。代表案例处理 848 条样本级记录、46 个字段、44 个已登记来源。主要质量结果：来源可追溯 PASS，实体 MATCH，科研适用性 REVIEW。能找到原始出处（官方 URL + accession + 本地缓存），但当前输出 **不适合** 直接做治疗响应建模。

**必须人工审核：** IHC 2+、跨库是否同一患者、结局域是否匹配、正式评测成绩。

**尚不能支持：** 无 Gold Set 的官方 SDTI 宣称；GTEx/OncoKB 等未接入库的假数据；把细胞系药敏当成患者疗效。

**复现入口：** 仓库前端 `http://127.0.0.1:8002/`；`POST /api/research/task`；`GET /api/task/status/{id}`；导出 `/export/{format}`；千问 `POST /api/agent/qwen-sessions`。源代码与依赖见仓库 `README_START_HERE.md`。

---

## 你下一步只需做的三件事

1. **截图：** 按文首表 1–9（图10 可选）。  
2. **用百炼跑一次千问真实任务**（同一研究问题），把「执行模式 / 工具表 / 质量门」截给我，我帮你改 P7、P18 里「待实测」两行。  
3. **决定 P18 外部对照：** 若只做本系统内部变体，不必克隆其他 GitHub；若要 cBioPortal 手工对照，导出一份 METABRIC 临床表截图即可，不必跑 BEIR。
